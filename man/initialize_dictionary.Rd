% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_graph.R
\name{initialize_dictionary}
\alias{initialize_dictionary}
\title{Initialize Dictionary Matrix}
\usage{
initialize_dictionary(S, D, method = "kmeans", eps = 1e-06, seed = NULL)
}
\arguments{
\item{S}{Data matrix (N x K) of cell type scores}

\item{D}{Number of mixture components (dictionary atoms)}

\item{method}{Initialization method. Options: "kmeans", "random", "uniform"}

\item{eps}{Floor value for simplex constraint. Default: 1e-6}

\item{seed}{Random seed for reproducibility. Default: NULL}
}
\value{
Dictionary matrix (K x D) with columns on the simplex
}
\description{
Creates an initial dictionary matrix B using k-means clustering on the
cell type score matrix S. This provides a data-driven initialization
for the optimization.
}
\details{
For \code{method = "kmeans"}: Performs k-means clustering on S and uses
cluster centers as initial dictionary atoms after normalizing columns.

For \code{method = "random"}: Samples from Dirichlet distribution.

For \code{method = "uniform"}: Uses uniform distribution on simplex.
}
\examples{
\dontrun{
# Create example data
S <- matrix(rexp(100 * 10), nrow = 100, ncol = 10)

# Initialize with k-means
B <- initialize_dictionary(S, D = 3, method = "kmeans")

# Check constraints
colSums(B)  # Should be all 1s
min(B)      # Should be >= eps
}
}
