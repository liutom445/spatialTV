% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{components_to_celltypes}
\alias{components_to_celltypes}
\title{Convert Component Mixtures to Cell Type Proportions}
\usage{
components_to_celltypes(X, B, normalize = TRUE)
}
\arguments{
\item{X}{Mixture matrix (N x D)}

\item{B}{Dictionary matrix (K x D)}

\item{normalize}{If TRUE, normalize rows to sum to 1. Default: TRUE}
}
\value{
Matrix (N x K) of cell type proportions
}
\description{
Transforms the low-rank component representation (X) back to cell type
proportions using the dictionary (B).
}
\details{
The transformation is: \eqn{P = X B^T} followed by row normalization.
}
\examples{
\dontrun{
# After optimization
result <- fit_spatial_mixture(S, A, B, lambda = 2)

# Convert to cell type proportions
cell_type_props <- components_to_celltypes(result$X, result$B)

# Check dimensions
dim(cell_type_props)  # N x K
rowSums(cell_type_props)  # All 1s
}
}
